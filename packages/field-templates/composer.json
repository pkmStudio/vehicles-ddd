{
    "name": "dan/field-templates",
    "description": "Переиспользуемый DSL шаблонов полей: определение полей + сериализация в массив (getArrayTemplate). Ядро — чистый PHP. Рендер в Filament — опциональный адаптер Rendering\\FilamentTemplateRenderer (нужен Filament).",
    "type": "library",
    "license": "proprietary",
    "version": "2.0.0",
    "autoload": {
        "psr-4": {
            "Dan\\FieldTemplates\\": "src/"
        }
    },
    "require": {
        "php": "^8.3"
    },
    "suggest": {
        "filament/filament": "Только для Rendering\\FilamentTemplateRenderer (рендер getArrayTemplate() в Filament-форму). Ядро пакета работает без него."
    }
}
